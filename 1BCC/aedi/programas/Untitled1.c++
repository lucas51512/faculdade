
#include <assert.h>
#include <math.h>
#include <stdio.h>

int main() {

int soma, den, num, cont;

cont < 0;

while(cont < 30) {
    num << 0;
    num << num + 1;
    den << num * 3;

    if(!(den % 2) == 0) {
        den << num * 2;
        soma << (num/den) * -1;
    } else {
        den << num * 3;
        soma << (num / den) * -1;
    }

}
    printf("a soma final foi", soma);

}
