//Alg prova;

var soma, den, num, cont: inteiro;  

INICIO

cont <- 1;
num <- 0;
soma < - 0;
den <- 0;

enquanto (cont <= 10) {

num <- num + 2;
den <- 2 ^ cont;

soma <- soma + (num/den) * -1;

imprima(soma + '=' + num '/' + den);

cont = cont + 1;
}

FIM.
