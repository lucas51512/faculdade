Algorítmos para calcular numeros primos;

VAR NUM <- 0 DIV <- 0: inteiros;

INICIO
    leia(num);
    divisor <- num - 1;

        enquanto(num % divisor != 0)
            divisor <- divisor - 1;
        se(resto)        

FIM.