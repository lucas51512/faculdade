// "Área da Circunferência + Calculo";

    VAR AREA, RAIO: real;

INICIO
    ESCREVA("Informe o Raio");
    LEIA(RAIO);

    AREA <- PI * RAIO ^ 2;

    ESCREVA("A área calculada foi ", AREA);
    ESCREVA("O calculo executado foi AREA = PI * " + RAIO + " ^ 2");

FIM.